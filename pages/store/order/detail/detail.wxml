<!-- 编号 -->
<view class="order_id">
    订单号:{{order_id}}
<view wx:if="{{is_pay==false}}" style="color:red;">未支付</view>
<view wx:else  style="color:green;" >已支付</view>
</view>
<!-- 购买的东西的清单 -->
<view class="shop666">商品清单</view>
<!-- 列表展示 -->
<view class="nihap">
    <view wx:for="{{shop_list}}" class="showlist">
        <image src="{{item.img}}"  />
        <view class="des">
            {{item.title}}
    <view class="price">
            {{item.price}} 元 × 1
    </view>
        </view>
    </view>
</view>
<view class="all">合计：{{all_price}}元</view>
<view class="info">配送信息</view>
<view class="address">
    <image src="/res/icons/lc.png"  />
    老王 15762300000
<view class="detailed" >
    山东省日照市烟北路55号</view>
</view>
<view class="pay">
    <view class="payinfo">请选择支付方式</view>
    <radio-group bindchange="radioChange">
        <view>
            <radio checked="true" value="0"  />
            会员卡余额( 可用 {{hava_money}}元 )
      </view>
            <view >
                <radio value="1"  />
                微信支付</view>
            </radio-group>
            <view class="submit">
                <button size="mini" bindtap="pay">付款</button>
            </view>
        </view>

